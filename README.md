# my zsh dotfiles
> 使用zinit 插件管理工具

1. `git clone https://github.com/sunowsir/zsh.dotfiles`
2. `cd zsh.dotfiles`
3. `git submodule update --init --recursive`
2. `cp ./.zshrc ~/`
3. `cp -r ./zsh ~/.config/`
